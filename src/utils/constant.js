// src/config.js or similar
export const BASE_URL = import.meta.env.VITE_API_URL || "http://localhost:5000";

// Note: In Production, VITE_API_URL will be "/api"
// In Local, it will fall back to "http://localhost:5000"
